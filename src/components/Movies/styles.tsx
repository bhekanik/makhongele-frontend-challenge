import { Container } from '@material-ui/core';
import styled from 'styled-components';

export const MoviesWrapper = styled(Container)`
  margin-top: 2rem;
  height: 100vh;
  overflow: hidden;
`;

export const ButtonsWrapper = styled.div`
  display: flex;
  gap: 1rem;
`;
