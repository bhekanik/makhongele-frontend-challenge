import { Movies } from 'src/components/Movies';
import { Layout } from 'src/layouts/Layout';

const Home = () => {
  return (
    <Layout>
      <Movies />
    </Layout>
  );
};

export default Home;
